ENVIRONMENT=local
# CORS origins as a JSON list; defaults to the Vite dev origin if unset
# CORS_ORIGINS=["http://localhost:5173"]
# Async SQLAlchemy DSN; defaults to the compose db service
# DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/regrag
