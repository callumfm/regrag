# Environment: dev, test, prod
ENVIRONMENT=test

# PostgreSQL Database (compose db service)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASS=postgres
DB_NAME=regrag

# CORS origins as a JSON list (Vite dev origin)
CORS_ORIGINS=["http://localhost:5173"]
