ENVIRONMENT=dev
# CORS origins as a JSON list; defaults to the Vite dev origin if unset
# CORS_ORIGINS=["http://localhost:5173"]
# Postgres connection; defaults match the compose db service
# DB_HOST=localhost
# DB_PORT=5432
# DB_USER=postgres
# DB_PASS=postgres
# DB_NAME=regrag
